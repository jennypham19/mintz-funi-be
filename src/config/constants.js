// src/config/constants.js
const roomStatuses = ['clean', 'dirty', 'in_progress', 'maintenance'];
const taskStatuses = ['pending', 'in_progress', 'completed', 'cancelled'];
const userRoles = ['admin', 'manager', 'staff'];
// Thêm các hằng số khác nếu cần

module.exports = {
  roomStatuses,
  taskStatuses,
  userRoles,
};